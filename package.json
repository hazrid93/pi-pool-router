{
  "name": "pi-pool-router",
  "version": "1.0.0",
  "description": "Pi/omp in-process extension that pools multiple LLM backends behind a single provider — handles routing, failover, health checks, and latency-aware selection",
  "type": "module",
  "main": "src/index.ts",
  "omp": {
    "extensions": ["./src/index.ts"]
  },
  "pi": {
    "extensions": ["./src/index.ts"]
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "@mariozechner/pi-ai": "*",
    "@mariozechner/pi-coding-agent": "*"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
